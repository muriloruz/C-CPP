#include <iostream>
using namespace std;
void exemplo(){

       int counter = 5;
       while(counter--) cout<< endl << "I am an awesome program" << endl;

}
void outroExemplo(){
	/* we will count the numbers here */
	int evens = 0, odds = 0;

	/* we will store the incoming numbers here */
	int number;

	/* read first number */
	cout<<endl<<"Enter with the number: \n---> ";
	cin >> number;

	/* 0 terminates execution */
	while (number != 0) {
		/* check if the number is odd */
		if (number % 2 == 1)
			/* increase „odd” counter */
			odds++;
		else
			/* increase „even” counter */
			evens++;
		/* read next number */
		cin >> number;
	}
	/* print results */
	cout << "Even numbers: " << evens << endl;
	cout << "Odd numbers: " << odds << endl;

}
int main(){
	int condi;
	//while são ifs que repetem.
	while(true){
		cout<<"Loop infinito, digite a senha pra sair: ";
		cin>>condi;
		if(condi == 324){
			cout<<"Saiu do Loop!";
			break;
		}
	}
	outroExemplo();
	exemplo();
}
