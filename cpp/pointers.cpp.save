#include <iostream>
#include <locale.h>
using namespace std;
int exempUm(int a){
    int *ptr;
    ptr = &a;
    cout<<"O endereço da variável 'ptr' é: "<<ptr<<endl;
    return *ptr;
}
int exempSize(int a){
    int *newprt;
    newprt = &a;
    int i = sizeof newprt;
    return i;
}
int main(){
    setlocale(LC_ALL,"Portuguese");
    cout<<"O valor da variável 'a' é: "<<exempUm(5)<<endl;
    cout<<"O tamanho do"<<exempSize(5);
}
